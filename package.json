{
  "name": "image-vision-mcp",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node --format esm --external @anthropic-ai/sdk --external @modelcontextprotocol/sdk --external zod --minify",
    "build:bundle": "bun build src/index.ts --outfile dist/index.js --target node --format esm --minify",
    "dev": "bun run src/index.ts",
    "package": "bun run build:bundle && bun run scripts/package-release.ts",
    "test": "bun test --max-concurrency=1",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
